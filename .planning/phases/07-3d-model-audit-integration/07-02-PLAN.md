---
phase: 07-3d-model-audit-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Editor/VeilBreakers/VB_ModelAuditor.cs
  - Assets/Resources/CharacterSelect/HeroDisplayConfigs/NyxDisplayConfig.asset
  - Assets/Resources/CharacterSelect/HeroDisplayConfigs/OrionDisplayConfig.asset
  - Assets/Resources/CharacterSelect/HeroDisplayConfigs/SeraphinaDisplayConfig.asset
autonomous: false
requirements: MODEL-03, MODEL-05
user_setup: []

must_haves:
  truths:
    - "At least 1 champion monster model displays alongside its hero in CharSelect"
    - "Monster model file sizes checked against budget targets (50K tris hero, 30K tris monster)"
    - "All 3 monsters have prefabs assignable to HeroDisplayConfig.championModelPrefab"
  artifacts:
    - path: "Assets/Editor/VeilBreakers/VB_ModelAuditor.cs"
      provides: "Extended with WireChampionModels MenuItem"
    - path: "Assets/Resources/CharacterSelect/HeroDisplayConfigs/NyxDisplayConfig.asset"
      provides: "championModelPrefab referencing Bloodshade v4"
      contains: "championModelPrefab"
    - path: "Assets/Resources/CharacterSelect/HeroDisplayConfigs/OrionDisplayConfig.asset"
      provides: "championModelPrefab referencing Voltgeist v4"
      contains: "championModelPrefab"
    - path: "Assets/Resources/CharacterSelect/HeroDisplayConfigs/SeraphinaDisplayConfig.asset"
      provides: "championModelPrefab referencing Grimthorn v4"
      contains: "championModelPrefab"
  key_links:
    - from: "VB_ModelAuditor.cs"
      to: "Assets/Art/Models/Monsters/*/model_v4_pbr.glb"
      via: "AssetDatabase.LoadAssetAtPath<GameObject>"
      pattern: "model_v4_pbr\\.glb"
    - from: "VB_ModelAuditor.cs"
      to: "HeroDisplayConfig.championModelPrefab"
      via: "SerializedObject property assignment"
      pattern: "championModelPrefab.*objectReferenceValue"
    - from: "HeroDisplayConfig.championModelPrefab"
      to: "HeroStageController"
      via: "config.championModelPrefab consumed by left panel display"
      pattern: "championModelPrefab"
---

<objective>
Wire champion monster model prefabs into HeroDisplayConfig SOs and verify model polycount budgets.

Purpose: Each hero has a canonical starter monster that should display in CharSelect's left panel. This plan wires the monster prefabs and checks budget compliance.

Output: VB_ModelAuditor extended with WireChampionModels MenuItem + 3 HeroDisplayConfig SOs updated with championModelPrefab references.
</objective>

<execution_context>
@C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-3d-model-audit-integration/07-CONTEXT.md

<interfaces>
<!-- Key types the executor needs -->

From Assets/Scripts/Data/HeroDisplayConfig.cs:
```csharp
[Header("Champion Monster")]
[Tooltip("null = data-only display in left panel")]
public GameObject championModelPrefab;
public Vector3 championOffset = new Vector3(0.5f, 0f, 0.3f);
[Range(0.1f, 1f)]
public float championScale = 0.35f;
public AnimationClip championIdleClip;
```

Hero-Monster champion mapping (from heroes.json `starter_monster_id`):
- Nyx -> bloodshade: Assets/Art/Models/Monsters/Bloodshade/model_v4_pbr.glb
- Orion -> voltgeist: Assets/Art/Models/Monsters/Voltgeist/model_v4_pbr.glb
- Seraphina -> grimthorn: Assets/Art/Models/Monsters/Grimthorn/model_v4_pbr.glb
- Vex -> skitter_teeth: NO MODEL in Assets/Art/Models/Monsters/ (only Bloodshade, Grimthorn, Voltgeist exist)

HeroDisplayConfig SO paths:
- Assets/Resources/CharacterSelect/HeroDisplayConfigs/NyxDisplayConfig.asset (heroId: "nyx")
- Assets/Resources/CharacterSelect/HeroDisplayConfigs/OrionDisplayConfig.asset (heroId: "orion")
- Assets/Resources/CharacterSelect/HeroDisplayConfigs/SeraphinaDisplayConfig.asset (heroId: "seraphina")

From HeroStageController.cs (line 284):
```csharp
// Champion model is now displayed in the panel's model viewer, not on the hero stage.
// _currentChampion field is kept for cleanup compatibility but not instantiated here.
```
Champion models are displayed via championModelPrefab in the left data panel, NOT on the main hero stage.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WireChampionModels MenuItem and check budgets</name>
  <files>Assets/Editor/VeilBreakers/VB_ModelAuditor.cs</files>
  <action>
Extend `VB_ModelAuditor.cs` (created in Plan 01) with a third MenuItem method.

**MenuItem 3: "VeilBreakers/Wire Champion Monsters"**

Define the hero-to-champion monster mapping:
```
"nyx"        -> "Assets/Art/Models/Monsters/Bloodshade/model_v4_pbr.glb"
"orion"      -> "Assets/Art/Models/Monsters/Voltgeist/model_v4_pbr.glb"
"seraphina"  -> "Assets/Art/Models/Monsters/Grimthorn/model_v4_pbr.glb"
```

Implementation:
1. For each mapping:
   - Load HeroDisplayConfig SO from `Assets/Resources/CharacterSelect/HeroDisplayConfigs/{HeroName}DisplayConfig.asset`
   - Load the GLB model asset via `AssetDatabase.LoadAssetAtPath<GameObject>(glbPath)`
   - If null: log error, skip
   - Use `SerializedObject` + `Undo.RecordObject` to set `championModelPrefab` to the model's root GameObject
   - Log: "Wired {heroId} championModelPrefab -> {monsterName} v4"
2. For Vex: log warning "Vex champion (skitter_teeth) has no model in Assets/Art/Models/Monsters/ -- keeping null"
3. Print summary: "Wired 3/4 champion monsters. Vex champion missing."

**MenuItem 4: "VeilBreakers/Check Model Budgets"**

Simple budget check using file size as proxy (polycount requires runtime mesh read):
1. For each v4 model:
   - Get file size via `System.IO.FileInfo`
   - Classify: hero budget = 50K tris, monster budget = 30K tris
   - Log size vs estimated range (note: file size is not polycount, flag if >25MB as potentially over-budget)
2. Print a table: Character | Type (hero/monster) | Budget | FileSize | Status (OK/REVIEW)
3. Log disclaimer: "File size is a rough proxy. True polycount check requires runtime mesh read (vertexCount). Use Unity Profiler or FBX viewer for exact counts."

Code style per CLAUDE.md: `k` prefix for constants, `_` prefix for privates.
  </action>
  <verify>
    <automated>Compile check: recompile Unity project and verify zero errors in VB_ModelAuditor.cs</automated>
  </verify>
  <done>
    VB_ModelAuditor.cs has two new MenuItem methods (WireChampionModels, CheckModelBudgets). Compiles without errors. Menu items visible under VeilBreakers/ menu.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Run champion wiring and verify in CharSelect</name>
  <files>
    Assets/Resources/CharacterSelect/HeroDisplayConfigs/NyxDisplayConfig.asset
    Assets/Resources/CharacterSelect/HeroDisplayConfigs/OrionDisplayConfig.asset
    Assets/Resources/CharacterSelect/HeroDisplayConfigs/SeraphinaDisplayConfig.asset
  </files>
  <action>
Execute in Unity Editor:

1. **Run "VeilBreakers/Wire Champion Monsters"** -- assigns Bloodshade to Nyx, Voltgeist to Orion, Grimthorn to Seraphina.

2. **Run "VeilBreakers/Check Model Budgets"** -- logs budget status for all v4 models.

3. **Verify .asset files**: Read each modified asset YAML and confirm `championModelPrefab` has non-zero fileID and valid GUID reference.

4. **Visual verification**: If Unity Editor is available with CharSelect scene loaded:
   - Enter play mode, navigate to CharSelect
   - Select Nyx -- confirm left panel shows Bloodshade model (not empty/null)
   - Select Orion -- confirm left panel shows Voltgeist model
   - Select Seraphina -- confirm left panel shows Grimthorn model
   - Take screenshot of Nyx with champion visible
  </action>
  <verify>
    <automated>Read NyxDisplayConfig.asset, OrionDisplayConfig.asset, SeraphinaDisplayConfig.asset -- confirm championModelPrefab has non-zero fileID for all 3. Also confirm budget report appears in console logs.</automated>
  </verify>
  <what-built>Champion monster models wired to 3 hero DisplayConfigs, budget check completed</what-built>
  <how-to-verify>
    1. Read NyxDisplayConfig.asset -- championModelPrefab field has non-zero fileID
    2. Read OrionDisplayConfig.asset -- championModelPrefab field has non-zero fileID
    3. Read SeraphinaDisplayConfig.asset -- championModelPrefab field has non-zero fileID
    4. If Unity accessible: play CharSelect, select Nyx, check left panel shows Bloodshade model
  </how-to-verify>
  <resume-signal>Type "approved" if champion models appear correctly, or describe issues</resume-signal>
  <done>
    At least 1 champion monster (ideally all 3) displays in CharSelect left panel alongside its hero. Budget check report produced. All .asset files have valid championModelPrefab references.
  </done>
</task>

</tasks>

<verification>
1. VB_ModelAuditor.cs compiles with zero errors
2. "VeilBreakers/Wire Champion Monsters" menu item exists
3. NyxDisplayConfig.asset championModelPrefab != {fileID: 0}
4. OrionDisplayConfig.asset championModelPrefab != {fileID: 0}
5. SeraphinaDisplayConfig.asset championModelPrefab != {fileID: 0}
6. Budget check report logged for all 6 v4 models
</verification>

<success_criteria>
- 3 champion monsters (Bloodshade, Voltgeist, Grimthorn) wired into HeroDisplayConfig.championModelPrefab
- Model budget report produced with status per model
- Vex champion gap documented (skitter_teeth not in model library)
- At least 1 champion monster visually verified in CharSelect (or .asset file verified)
</success_criteria>

<output>
After completion, create `.planning/phases/07-3d-model-audit-integration/07-02-SUMMARY.md`
</output>
