---
phase: 05-title-screen-aaa-rebuild
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/UI/Core/NativeBlurTest.cs
autonomous: true
requirements:
  - TITLE-07

must_haves:
  truths:
    - "Native FilterFunction.Blur() renders a visible blur effect on a UI Toolkit VisualElement"
    - "Blur renders correctly at runtime in the title screen scene context"
  artifacts:
    - path: "Assets/Scripts/UI/Core/NativeBlurTest.cs"
      provides: "Smoke test for FilterFunction.Blur() in UI Toolkit"
      contains: "FilterFunction.Blur"
  key_links:
    - from: "NativeBlurTest"
      to: "UnityEngine.UIElements.FilterFunction"
      via: "style.filter assignment"
      pattern: "FilterFunction\\.Blur"
---

<objective>
Test native FilterFunction.Blur() in Unity 6000.3 to verify it renders correctly in the title screen's UI Toolkit context before building production blur effects.

Purpose: The research indicates FilterFunction.Blur is available but has not been tested in this project's runtime. Testing early avoids wasting time building effects on a broken foundation.
Output: NativeBlurTest.cs (disposable test script), screenshot evidence of blur working
</objective>

<execution_context>
@C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-title-screen-aaa-rebuild/05-RESEARCH.md

<interfaces>
<!-- Unity 6000.3 FilterFunction API (from research) -->

Expected API:
```csharp
// Native blur on a VisualElement
visualElement.style.filter = new FilterFunction[] { FilterFunction.Blur(8f) };
visualElement.style.backgroundColor = someColor;
```

From Assets/Scripts/UI/Core/TitleScreenVFX.cs:
- TitleScreenVFX sits on the same GameObject as MainMenuController
- It accesses the UIDocument via GetComponent<UIDocument>() in Awake
- The root VisualElement is obtained via _document.rootVisualElement
- VFX elements are added as children of the root or specific containers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and test FilterFunction.Blur smoke test</name>
  <files>Assets/Scripts/UI/Core/NativeBlurTest.cs</files>
  <read_first>
    - Assets/Scripts/UI/Core/TitleScreenVFX.cs (first 50 lines for namespace and MonoBehaviour pattern)
  </read_first>
  <action>
    Create `Assets/Scripts/UI/Core/NativeBlurTest.cs` as a disposable MonoBehaviour test script in namespace `VeilBreakers.UI.Core`.

    The script must:
    1. In Start(), find the UIDocument (use `FindFirstObjectByType<UIDocument>()`)
    2. Get the root VisualElement
    3. Create a test VisualElement with:
       - `position = Position.Absolute`
       - `width = 200; height = 200`
       - `left = 100; top = 100`
       - `backgroundColor = new Color(1f, 0.4f, 0.12f, 0.4f)` (orange glow color)
       - `pickingMode = PickingMode.Ignore`
    4. Apply native blur: `testElement.style.filter = new FilterFunction[] { FilterFunction.Blur(12f) }`
    5. Add the test element to the root
    6. Log success or failure to console via ErrorLogger: `"NativeBlurTest: Blur applied successfully"` or error message with exception details

    Use Context7 to verify the FilterFunction.Blur API signature before writing:
    - Call resolve-library-id for `/needle-mirror/com.unity.ui`
    - Call query-docs with "FilterFunction Blur style.filter VisualElement"
    - If Context7 does not have FilterFunction docs, read the Unity UI Toolkit package source in `Packages/` to find the actual API

    After creating the script:
    1. Add it as a component on the same GameObject as TitleScreenVFX in the title scene
    2. Use `unity_editor action=recompile` to compile
    3. Use `unity_editor action=enter_play_mode` to test
    4. Use `unity_editor action=screenshot` to capture the result
    5. Verify the screenshot shows a blurred orange rectangle

    If FilterFunction.Blur does not compile or does not render:
    - Log the exact error and API shape found
    - Note in the file that native blur is NOT available, so texture-based glow must be used instead
    - This is a valid outcome -- the purpose is to test, not to force it to work
  </action>
  <verify>
    <automated>cd "C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent" && grep -c "FilterFunction.Blur" Assets/Scripts/UI/Core/NativeBlurTest.cs && grep -c "style.filter" Assets/Scripts/UI/Core/NativeBlurTest.cs</automated>
  </verify>
  <acceptance_criteria>
    - File `Assets/Scripts/UI/Core/NativeBlurTest.cs` exists
    - Contains `namespace VeilBreakers.UI.Core`
    - Contains `FilterFunction.Blur` usage
    - Contains `style.filter` assignment
    - Script compiles in Unity
    - Screenshot captured showing blur test result (or error documented in file)
  </acceptance_criteria>
  <done>FilterFunction.Blur() tested in title screen context. Screenshot captured. If it works: blur confirmed available for production effects. If it fails: documented limitation, texture-based glow stays as fallback.</done>
</task>

</tasks>

<verification>
1. NativeBlurTest.cs exists: `grep -c "FilterFunction.Blur" Assets/Scripts/UI/Core/NativeBlurTest.cs`
2. Compiles in Unity: `unity_editor action=recompile`
3. Visual test: screenshot captured via `unity_editor action=screenshot`
</verification>

<success_criteria>
- FilterFunction.Blur API verified (works or documented as unavailable)
- Test script compiles and runs
- Screenshot evidence captured
- Decision recorded: native blur available for production OR texture-based glow required
</success_criteria>

<output>
After completion, create `.planning/phases/05-title-screen-aaa-rebuild/05-02-SUMMARY.md`
</output>
