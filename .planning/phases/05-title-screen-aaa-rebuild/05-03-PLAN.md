---
phase: 05-title-screen-aaa-rebuild
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - Assets/Scripts/UI/VFX/TitleScreenParticles.cs
  - Assets/Scripts/UI/VFX/TitleScreenLightning.cs
  - Assets/Scripts/UI/VFX/TitleScreenLogoVFX.cs
  - Assets/Scripts/UI/VFX/TitleScreenAtmosphere.cs
  - Assets/Scripts/UI/VFX/TitleScreenVideoBackground.cs
  - Assets/Scripts/UI/Core/TitleScreenVFX.cs
autonomous: true
requirements:
  - TITLE-03
  - TITLE-04
  - TITLE-05

must_haves:
  truths:
    - "TitleScreenVFX is reduced to a slim orchestrator (~200 lines) that delegates to focused subsystems"
    - "All existing visual behavior is preserved -- screenshot comparison before/after shows identical results"
    - "Each subsystem manages one visual domain with a narrow public API (Start/Stop/SetIntensity)"
  artifacts:
    - path: "Assets/Scripts/UI/VFX/TitleScreenParticles.cs"
      provides: "Ember, ash, spark, micro-spark particle management"
      contains: "class TitleScreenParticles"
    - path: "Assets/Scripts/UI/VFX/TitleScreenLightning.cs"
      provides: "Lightning strikes and flash overlay"
      contains: "class TitleScreenLightning"
    - path: "Assets/Scripts/UI/VFX/TitleScreenLogoVFX.cs"
      provides: "Logo breathing, pulse, glow, aura, interactions"
      contains: "class TitleScreenLogoVFX"
    - path: "Assets/Scripts/UI/VFX/TitleScreenAtmosphere.cs"
      provides: "Fog, vignette, grunge, atmosphere gradient"
      contains: "class TitleScreenAtmosphere"
    - path: "Assets/Scripts/UI/VFX/TitleScreenVideoBackground.cs"
      provides: "Ping-pong video loop with RenderTextures"
      contains: "class TitleScreenVideoBackground"
    - path: "Assets/Scripts/UI/Core/TitleScreenVFX.cs"
      provides: "Slim orchestrator with StartVFX/StopVFX/SetIntensity/OnButtonHovered/SparkBurst"
      min_lines: 100
      max_lines: 400
  key_links:
    - from: "TitleScreenVFX"
      to: "TitleScreenParticles, TitleScreenLightning, TitleScreenLogoVFX, TitleScreenAtmosphere, TitleScreenVideoBackground"
      via: "SerializedField references"
      pattern: "\\[SerializeField\\].*TitleScreen(Particles|Lightning|LogoVFX|Atmosphere|VideoBackground)"
---

<objective>
Decompose the 3146-line TitleScreenVFX god class into 5 focused MonoBehaviour subsystems plus a slim orchestrator.

Purpose: The god class is unmaintainable and blocks all other AAA enhancement work (blur integration, registry integration, VFX container). Decomposition must happen before anything else can be layered on top.
Output: 5 new subsystem files + slimmed TitleScreenVFX orchestrator
</objective>

<execution_context>
@C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-title-screen-aaa-rebuild/05-RESEARCH.md
@.planning/phases/05-title-screen-aaa-rebuild/05-01-SUMMARY.md

<interfaces>
<!-- UITextureRegistry from Plan 01 -->
From Assets/Scripts/UI/Core/UITextureRegistry.cs:
```csharp
namespace VeilBreakers.UI.Core
{
    public class UITextureRegistry
    {
        public Texture2D Register(Texture2D tex);
        public void DestroyAll();
        public void Unregister(Texture2D tex);
        public int Count { get; }
    }
}
```

<!-- TitleScreenVFX public API that MUST be preserved -->
From Assets/Scripts/UI/Core/TitleScreenVFX.cs:
```csharp
public class TitleScreenVFX : MonoBehaviour
{
    // Public API (must remain identical)
    public void StartVFX()        // line 2080
    public void StopVFX()         // line 2087
    public void SetIntensity(float intensity)  // line 3034
    public void OnButtonHovered(bool isHovered, bool isPrimary)  // line 3047
    public void SparkBurst(Vector2 position, int count = 5)      // line 3055
}
```

<!-- TitleScreenVFX subsystem boundaries (from research analysis) -->

Decomposition targets:
| Subsystem | Est. Lines | Key Methods | SerializFields |
|-----------|-----------|-------------|----------------|
| TitleScreenParticles | ~800 | CreateEmber/Ash/Spark/MicroSpark/Smoke, UpdateEmber/Ash/Spark, UpdateParticles coroutine, ResetPositions | _emberCount, _emberSpeedMin/Max, _emberSizeMin/Max, _ashCount, _ashSpeed*, _sparkCount, _microSparkCount, _windStrength, _turbulenceStrength, _spawnMarginBottom/Sides, particle textures |
| TitleScreenLightning | ~400 | CreateLightningLayer, CreateLightningStrikeElement, UpdateLightning, TriggerLightningStrike, ScheduleNextLightning | _lightningInterval*, _lightningStrikeDuration*, _lightningIntensity, _lightningBoltTextureA/B |
| TitleScreenLogoVFX | ~350 | SetupLogoInteractions, UpdateLogo, TriggerLogoPulse, CreateLogoAura, SpawnLogoSmokeBurst, OnLogoEnter/Leave/PointerDown | _logoGlowBaseOpacity, _logoGlowHoverOpacity, _logoGlowClickOpacity, _logoPulseDuration, _logoGlowTexture |
| TitleScreenAtmosphere | ~250 | CreateAtmosphereLayer, CreateVignetteLayer, CreateVignetteCorner, CreateGrungeOverlay, CreateFogLayers | _vignetteOpacity, _grungeOpacity, _grungeTexture |
| TitleScreenVideoBackground | ~400 | SetupVideoBackground, SetupPingPongEvents, OnForward/ReversedVideoPrepared/Ended, ApplyBackground | _backgroundPortalTexture, _backgroundDarken, video RenderTextures |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract TitleScreenParticles and TitleScreenLightning subsystems</name>
  <files>
    Assets/Scripts/UI/VFX/TitleScreenParticles.cs
    Assets/Scripts/UI/VFX/TitleScreenLightning.cs
    Assets/Scripts/UI/Core/TitleScreenVFX.cs
  </files>
  <read_first>
    - Assets/Scripts/UI/Core/TitleScreenVFX.cs (FULL file -- 3146 lines, must read completely for accurate extraction)
    - Assets/Scripts/UI/Core/UITextureRegistry.cs (from Plan 01, for texture tracking)
  </read_first>
  <action>
    This is the first half of the decomposition. Extract two subsystems from TitleScreenVFX.

    **BEFORE ANY EDITS:**
    1. Read the ENTIRE TitleScreenVFX.cs file
    2. Create the directory `Assets/Scripts/UI/VFX/` if it does not exist
    3. Take a screenshot of the current title screen for before/after comparison: `unity_editor action=screenshot`

    **Create TitleScreenParticles.cs** (namespace `VeilBreakers.UI.VFX`):
    - MonoBehaviour on child GameObject (so it can use coroutines)
    - Move ALL particle-related SerializedField configs from TitleScreenVFX:
      - Ember: _emberCount, _emberSpeedMin/Max, _emberSizeMin/Max, _emberTexture, _emberAttractRadius/Strength/VerticalInfluence
      - Ash: _ashCount, _ashSpeedMin/Max, _ashSizeMin/Max, _ashTexture
      - Spark: _sparkCount, _sparkSpeedMin/Max, _sparkSizeMin/Max (currently 0, disabled)
      - MicroSpark: _microSparkCount, _microSparkSpeedMin/Max, _microSparkSizeMin/Max
      - Smoke: _smokeCount, _smokeSpeedMin/Max, _smokeSizeMin/Max, _smokeTexture
      - Wind: _windStrength, _windFrequency, _flickerSpeed, _turbulenceStrength, _spawnMarginBottom, _spawnMarginSides
    - Move ALL particle struct definitions (EmberParticle, AshParticle, SparkParticle, MicroSparkParticle, SmokeParticle)
    - Move ALL particle methods: CreateEmber, CreateAsh, CreateSpark, CreateMicroSpark, CreateSmoke, ResetEmberPosition, ResetAshPosition, ResetSparkPosition, ResetMicroSparkPosition, ResetSmokePosition, UpdateEmber, UpdateAsh, UpdateSpark, UpdateMicroSpark, UpdateSmoke, UpdateParticles coroutine, CenterInParent
    - Move BurstParticle struct and related methods: SpawnMonsterBurstAtMonster, UpdateBurstParticles, SpawnGlowPulse, UpdateGlowPulses, SpawnLogoSmokeBurst, SpawnTransientSmoke, UpdateTransientSmokes
    - Public API: `void Initialize(VisualElement host)`, `void StartEmission()`, `void StopEmission()`, `void SetIntensity(float intensity)`, `void SparkBurst(Vector2 position, int count)`
    - Use UITextureRegistry for any runtime textures created by particles
    - In OnDisable: stop emission, destroy any particle textures via registry

    **Create TitleScreenLightning.cs** (namespace `VeilBreakers.UI.VFX`):
    - MonoBehaviour on child GameObject
    - Move lightning-related SerializedFields: _lightningIntervalMin/Max, _lightningStrikeDurationMin/Max, _lightningIntensity, _lightningBoltTextureA/B
    - Move lightning methods: CreateLightningLayer, CreateLightningStrikeElement, UpdateLightning, TriggerLightningStrike, ScheduleNextLightning
    - Move lightning state fields (the _lightningLayer VisualElement, timing state, etc.)
    - Public API: `void Initialize(VisualElement host)`, `void Enable()`, `void Disable()`
    - In OnDisable: disable lightning, clean up VisualElements

    **CRITICAL CONSTRAINTS:**
    - The private struct types (EmberParticle, AshParticle, etc.) must be inside the subsystem that uses them
    - TitleScreenVFX still compiles after this extraction (remove extracted fields/methods, add [SerializeField] references to the new subsystems)
    - Use `[SerializeField] private TitleScreenParticles _particles;` etc. in the orchestrator
    - Preserve ALL `[SerializeField]` attributes on moved fields so Inspector values transfer
    - After extraction, compile check: `unity_editor action=recompile`

    The orchestrator (TitleScreenVFX) should temporarily still have the unextracted subsystems inline -- we extract them incrementally across this task and the next.
  </action>
  <verify>
    <automated>cd "C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent" && test -f Assets/Scripts/UI/VFX/TitleScreenParticles.cs && echo "particles OK" && test -f Assets/Scripts/UI/VFX/TitleScreenLightning.cs && echo "lightning OK"</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Scripts/UI/VFX/TitleScreenParticles.cs` exists with `namespace VeilBreakers.UI.VFX` and `class TitleScreenParticles : MonoBehaviour`
    - `Assets/Scripts/UI/VFX/TitleScreenLightning.cs` exists with `namespace VeilBreakers.UI.VFX` and `class TitleScreenLightning : MonoBehaviour`
    - TitleScreenParticles has public methods: Initialize, StartEmission, StopEmission, SetIntensity, SparkBurst
    - TitleScreenLightning has public methods: Initialize, Enable, Disable
    - TitleScreenVFX.cs has `[SerializeField] private TitleScreenParticles _particles;` field
    - TitleScreenVFX.cs has `[SerializeField] private TitleScreenLightning _lightning;` field
    - All extracted [SerializeField] fields are in their respective subsystems
    - Files compile without errors
  </acceptance_criteria>
  <done>TitleScreenParticles and TitleScreenLightning extracted as MonoBehaviours. TitleScreenVFX references them via [SerializeField]. All files compile. Before-screenshot captured for comparison.</done>
</task>

<task type="auto">
  <name>Task 2: Extract TitleScreenLogoVFX, TitleScreenAtmosphere, TitleScreenVideoBackground; slim orchestrator</name>
  <files>
    Assets/Scripts/UI/VFX/TitleScreenLogoVFX.cs
    Assets/Scripts/UI/VFX/TitleScreenAtmosphere.cs
    Assets/Scripts/UI/VFX/TitleScreenVideoBackground.cs
    Assets/Scripts/UI/Core/TitleScreenVFX.cs
  </files>
  <read_first>
    - Assets/Scripts/UI/Core/TitleScreenVFX.cs (current state after Task 1 extraction)
    - Assets/Scripts/UI/VFX/TitleScreenParticles.cs (from Task 1, for reference on pattern)
  </read_first>
  <action>
    Complete the decomposition by extracting the remaining three subsystems and slimming the orchestrator.

    **Create TitleScreenLogoVFX.cs** (namespace `VeilBreakers.UI.VFX`):
    - MonoBehaviour on child GameObject
    - Move logo SerializedFields: _logoGlowBaseOpacity, _logoGlowHoverOpacity, _logoGlowClickOpacity, _logoPulseDuration, _logoGlowTexture
    - Move logo methods: SetupLogoInteractions (renamed Initialize), UpdateLogo, TriggerLogoPulse, CreateLogoAura, SpawnLogoSmokeBurst (delegates to particles), OnLogoEnter, OnLogoLeave, OnLogoPointerDown, EnsureLogoGlow, EnsureLogoFxLayer, RemoveLogoArtifacts, AddLogoShadow
    - Move logo state: logo VisualElement references, aura state, breathing animation state
    - Public API: `void Initialize(VisualElement host)`, `void StartBreathing()`, `void StopBreathing()`, `void OnButtonHovered(bool isHovered, bool isPrimary)`
    - Mouse event registration: register OnLogoEnter/Leave/PointerDown on the logo element in Initialize

    **Create TitleScreenAtmosphere.cs** (namespace `VeilBreakers.UI.VFX`):
    - MonoBehaviour on child GameObject
    - Move atmosphere SerializedFields: _vignetteOpacity, _grungeOpacity, _grungeTexture
    - Move atmosphere methods: CreateAtmosphereLayer, CreateVignetteLayer, CreateVignetteCorner, CreateGrungeOverlay, CreateFogLayers, CreateTopVignette
    - Public API: `void Initialize(VisualElement host)`

    **Create TitleScreenVideoBackground.cs** (namespace `VeilBreakers.UI.VFX`):
    - MonoBehaviour on child GameObject
    - Move video SerializedFields: _backgroundPortalTexture, _backgroundDarken
    - Move video methods: ApplyBackground, SetupVideoBackground, SetupVideoBackgroundDeferred (coroutine), SetupPingPongEvents, OnForwardVideoPrepared, OnReversedVideoPrepared, OnForwardVideoEnded, OnReversedVideoEnded
    - Move video state: VideoPlayer references, RenderTexture references
    - Public API: `void Initialize(VisualElement host)`, `void StartPlayback()`

    **Slim the orchestrator (TitleScreenVFX.cs):**
    - Target: ~150-300 lines
    - Keep: Awake, Start, OnEnable, OnDisable, OnDestroy (lifecycle)
    - Keep: Initialize method that calls subsystem.Initialize(host)
    - Keep: public API (StartVFX, StopVFX, SetIntensity, OnButtonHovered, SparkBurst) as thin delegation wrappers
    - Keep: Settings binding (StartSettingsBinding, BindSettingsWhenReady, TryBindSettingsManager, etc.) -- these are orchestrator-level concerns
    - Keep: LoadTexturesFromResources -- loads shared textures, distributes to subsystems
    - Keep: OnGeometryChanged, InitializeDeferred, InitializeStaggered -- orchestration logic
    - Keep: _textures UITextureRegistry field and DestroyAll in OnDisable
    - Remove: All extracted methods and their associated fields
    - Replace with: [SerializeField] references to the 5 subsystems

    **After slimming:**
    1. Compile check: `unity_editor action=recompile`
    2. Enter play mode: `unity_editor action=enter_play_mode`
    3. Capture screenshot: `unity_editor action=screenshot`
    4. Compare with before-screenshot from Task 1 -- visual output should be identical
    5. Exit play mode: `unity_editor action=exit_play_mode`

    **If visuals break:** The most likely cause is initialization ordering. TitleScreenVFX.InitializeStaggered/InitializeDeferred must call subsystem.Initialize() at the right point in the lifecycle. Check that subsystems receive their VisualElement host reference before they try to create children.
  </action>
  <verify>
    <automated>cd "C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent" && test -f Assets/Scripts/UI/VFX/TitleScreenLogoVFX.cs && test -f Assets/Scripts/UI/VFX/TitleScreenAtmosphere.cs && test -f Assets/Scripts/UI/VFX/TitleScreenVideoBackground.cs && wc -l Assets/Scripts/UI/Core/TitleScreenVFX.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Scripts/UI/VFX/TitleScreenLogoVFX.cs` exists with `class TitleScreenLogoVFX : MonoBehaviour`
    - `Assets/Scripts/UI/VFX/TitleScreenAtmosphere.cs` exists with `class TitleScreenAtmosphere : MonoBehaviour`
    - `Assets/Scripts/UI/VFX/TitleScreenVideoBackground.cs` exists with `class TitleScreenVideoBackground : MonoBehaviour`
    - TitleScreenVFX.cs is under 400 lines (was 3146)
    - TitleScreenVFX.cs has 5 [SerializeField] subsystem references
    - TitleScreenVFX.cs public API unchanged: StartVFX, StopVFX, SetIntensity, OnButtonHovered, SparkBurst
    - All files compile without errors
    - Screenshot comparison shows identical visual output
  </acceptance_criteria>
  <done>TitleScreenVFX decomposed from 3146 lines to ~200-300 line orchestrator. Five focused subsystem MonoBehaviours created. Visual output identical before/after. All files compile.</done>
</task>

</tasks>

<verification>
1. Five subsystem files exist in Assets/Scripts/UI/VFX/
2. TitleScreenVFX.cs is under 400 lines
3. TitleScreenVFX.cs has [SerializeField] references to all 5 subsystems
4. TitleScreenVFX public API unchanged (StartVFX, StopVFX, SetIntensity, OnButtonHovered, SparkBurst)
5. Unity compiles: `unity_editor action=recompile`
6. Visual comparison: screenshot matches pre-decomposition screenshot
</verification>

<success_criteria>
- TitleScreenVFX decomposed from 3146 to ~200-300 lines
- 5 subsystem MonoBehaviours in Assets/Scripts/UI/VFX/
- Public API preserved (no external caller changes needed)
- Visual output identical before/after
- All files compile
</success_criteria>

<output>
After completion, create `.planning/phases/05-title-screen-aaa-rebuild/05-03-SUMMARY.md`
</output>
