---
phase: 05-game-flow-quality
plan: 01
type: execute
wave: 1
depends_on: ["04-04"]
files_modified:
  - Assets/Scripts/UI/Menus/MainMenuController.cs
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03

must_haves:
  truths:
    - "Bootstrap scene loads first, initializes all managers, then transitions to MainMenu with fade"
    - "Settings button on MainMenu fires OnSettingsClicked event which MainMenuBootstrap handles to open settings overlay"
    - "Full flow works: Bootstrap -> MainMenu -> CharacterSelect (all Phase 1-4 features) -> Embark cinematic -> Save -> Overworld"
    - "No flash of wrong scene during startup (Bootstrap handles splash timing)"
---

# Plan 05-01: Game Flow Verification & Fixes

## Tasks

1. Remove ShowSettings() TODO stub in MainMenuController (replace with comment explaining event flow)
2. Verify Bootstrap -> MainMenu -> CharacterSelect -> Overworld scene flow at code level
3. Verify no scene flash (Bootstrap splash timing, fade transitions)
