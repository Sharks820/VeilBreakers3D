---
phase: 06-character-select-aaa-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Vex.asset
  - Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Seraphina.asset
  - Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Orion.asset
  - Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Nyx.asset
  - Assets/Resources/CharacterSelect/HeroThemes/HeroTheme_Vex.asset
  - Assets/Resources/CharacterSelect/HeroThemes/HeroTheme_Seraphina.asset
  - Assets/Resources/CharacterSelect/HeroThemes/HeroTheme_Orion.asset
  - Assets/Resources/CharacterSelect/HeroThemes/HeroTheme_Nyx.asset
autonomous: true
requirements: [CHARSEL-04]

must_haves:
  truths:
    - "Each hero has a distinct VolumeProfile asset with unique post-processing overrides"
    - "HeroThemeConfig SOs reference their respective VolumeProfile assets (not null)"
    - "VolumeProfileTransitioner.LerpTo reads per-hero overrides from the assigned profiles"
  artifacts:
    - path: "Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Vex.asset"
      provides: "Vex VolumeProfile with warm bloom, moderate vignette"
      contains: "Bloom"
    - path: "Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Seraphina.asset"
      provides: "Seraphina VolumeProfile with ethereal bloom, soft vignette"
      contains: "Bloom"
    - path: "Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Orion.asset"
      provides: "Orion VolumeProfile with tight vignette, cool bloom"
      contains: "Bloom"
    - path: "Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Nyx.asset"
      provides: "Nyx VolumeProfile with heavy vignette, high bloom, chromatic aberration"
      contains: "Bloom"
  key_links:
    - from: "HeroTheme_Vex.asset"
      to: "VolProfile_Vex.asset"
      via: "volumeProfile field"
      pattern: "volumeProfile"
    - from: "VolumeProfileTransitioner.cs"
      to: "HeroThemeConfig.volumeProfile"
      via: "LerpTo target profile read"
      pattern: "theme\\.volumeProfile\\.TryGet"
---

<objective>
Create 4 per-hero VolumeProfile .asset files with distinct post-processing overrides, then assign them to the existing HeroThemeConfig ScriptableObjects.

Purpose: VolumeProfileTransitioner.LerpTo already reads from HeroThemeConfig.volumeProfile, but all 4 configs have it set to null. This forces the runtime default path with no per-hero differentiation. Creating and assigning real profiles enables the existing lerp system to produce distinct visual transitions between heroes.

Output: 4 VolumeProfile assets + 4 updated HeroThemeConfig assets with volumeProfile field populated.
</objective>

<execution_context>
@C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/OneDrive/Documents/VeilBreakers3DCurrent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-character-select-aaa-rebuild/06-CONTEXT.md

@Assets/Scripts/UI/CharacterSelect/VolumeProfileTransitioner.cs
@Assets/Scripts/UI/CharacterSelect/HeroThemeConfig.cs
@Assets/Resources/CharacterSelect/HeroThemes/HeroTheme_Vex.asset
@Assets/Resources/CharacterSelect/HeroThemes/HeroTheme_Seraphina.asset
@Assets/Resources/CharacterSelect/HeroThemes/HeroTheme_Orion.asset
@Assets/Resources/CharacterSelect/HeroThemes/HeroTheme_Nyx.asset

<interfaces>
<!-- VolumeProfileTransitioner reads from HeroThemeConfig.volumeProfile -->
<!-- If volumeProfile is null, it falls back to CopySourceToDestination (no visual change) -->

From HeroThemeConfig.cs (the fields VolumeProfileTransitioner reads):
```csharp
public VolumeProfile volumeProfile;          // Currently null on all 4 SOs
public float chromaticAberrationIntensity;   // Only Nyx > 0 (0.15)
```

From VolumeProfileTransitioner.CacheTargetValues():
```csharp
// Reads from theme.volumeProfile: Bloom (intensity, tint), Vignette (intensity, color),
// DepthOfField (gaussianStart, gaussianEnd), ColorAdjustments (saturation, colorFilter)
// ChromaticAberration intensity comes from HeroThemeConfig.chromaticAberrationIntensity directly
```

VolumeProfileTransitioner.AutoWireVolume() default values for reference:
- Bloom intensity: 0.5, tint: white
- Vignette intensity: 0.3, color: black
- ColorAdjustments saturation: 10, colorFilter: white
- ChromaticAberration: 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 per-hero VolumeProfile assets with distinct overrides</name>
  <files>
    Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Vex.asset
    Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Seraphina.asset
    Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Orion.asset
    Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Nyx.asset
  </files>
  <action>
Create 4 VolumeProfile .asset files as YAML (Unity SerializedObject format). Each profile must contain these URP overrides with hero-specific values:

**VolProfile_Vex** (warm, fiery):
- Bloom: intensity=0.8, tint=(1.0, 0.7, 0.5, 1.0) warm orange
- Vignette: intensity=0.25, color=(0.25, 0.1, 0.05, 1.0) dark brown
- DepthOfField: gaussianStart=10, gaussianEnd=30
- ColorAdjustments: saturation=15, colorFilter=(1.0, 0.95, 0.9, 1.0) warm white
- ChromaticAberration: intensity=0

**VolProfile_Seraphina** (ethereal, mystic):
- Bloom: intensity=1.0, tint=(0.8, 0.6, 1.0, 1.0) lavender
- Vignette: intensity=0.20, color=(0.14, 0.08, 0.2, 1.0) deep purple
- DepthOfField: gaussianStart=8, gaussianEnd=25
- ColorAdjustments: saturation=5, colorFilter=(0.95, 0.9, 1.0, 1.0) cool white
- ChromaticAberration: intensity=0

**VolProfile_Orion** (tight, focused):
- Bloom: intensity=0.6, tint=(0.6, 0.5, 1.0, 1.0) cool blue
- Vignette: intensity=0.40, color=(0.08, 0.06, 0.2, 1.0) dark blue
- DepthOfField: gaussianStart=6, gaussianEnd=20
- ColorAdjustments: saturation=12, colorFilter=(0.92, 0.92, 1.0, 1.0) blue-white
- ChromaticAberration: intensity=0

**VolProfile_Nyx** (unstable, chaotic):
- Bloom: intensity=1.2, tint=(1.0, 0.3, 0.35, 1.0) blood red
- Vignette: intensity=0.35, color=(0.22, 0.04, 0.04, 1.0) deep crimson
- DepthOfField: gaussianStart=5, gaussianEnd=18
- ColorAdjustments: saturation=20, colorFilter=(1.0, 0.9, 0.9, 1.0) warm red tint
- ChromaticAberration: intensity=0 (managed by HeroThemeConfig.chromaticAberrationIntensity=0.15)

Each .asset file needs:
1. Valid Unity YAML header (`%YAML 1.1`, `tag:unity3d.com,2011`)
2. A MonoBehaviour root with `m_Script` referencing `UnityEngine.Rendering.Universal.VolumeProfile` type GUID
3. Child ScriptableObject entries for each VolumeComponent (Bloom, Vignette, DepthOfField, ColorAdjustments, ChromaticAberration)
4. Each component with `active: 1` and override fields with `m_OverrideState: 1`

IMPORTANT: VolumeProfile assets in Unity 6 use GUID-based internal references. The simplest reliable approach is to use the Unity Editor to create them. Generate an Editor script that creates the profiles at runtime:
  - Create `Assets/Editor/CreateVolumeProfiles.cs` as a MenuItem script
  - It should create VolumeProfile assets with the specific overrides above using the URP API
  - The MenuItem saves them to `Assets/Resources/CharacterSelect/HeroThemes/`
  - After running the menu item, delete the editor script

Use the URP API pattern:
```csharp
var profile = ScriptableObject.CreateInstance<VolumeProfile>();
profile.name = "VolProfile_Vex";
var bloom = profile.Add<Bloom>(true); // true = overrides enabled
bloom.intensity.Override(0.8f);
bloom.tint.Override(new Color(1f, 0.7f, 0.5f));
// ... etc for each component
AssetDatabase.CreateAsset(profile, "Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Vex.asset");
```

After creating the profile assets, immediately update the 4 HeroThemeConfig .asset files to point their `volumeProfile` field to the newly created profiles. The HeroThemeConfig YAML uses `{fileID: ..., guid: ..., type: 2}` format for ScriptableObject references. Use AssetDatabase API to get the correct GUID after creation.
  </action>
  <verify>
    <automated>test -f Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Vex.asset && test -f Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Seraphina.asset && test -f Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Orion.asset && test -f Assets/Resources/CharacterSelect/HeroThemes/VolProfile_Nyx.asset && echo "PASS: All 4 VolumeProfile assets exist"</automated>
  </verify>
  <done>
    - 4 VolumeProfile .asset files exist in Assets/Resources/CharacterSelect/HeroThemes/
    - Each profile contains Bloom, Vignette, DepthOfField, ColorAdjustments overrides with hero-specific values
    - HeroThemeConfig .asset files updated with volumeProfile references (no longer null)
    - Editor creation script cleaned up (deleted after use)
  </done>
</task>

</tasks>

<verification>
1. All 4 VolumeProfile assets exist and contain Bloom + Vignette + ColorAdjustments overrides
2. All 4 HeroThemeConfig .asset files reference their VolumeProfile (not fileID: 0)
3. No compilation errors (editor script removed after asset creation)
4. VolumeProfileTransitioner.CacheTargetValues will now read per-hero values instead of falling back to CopySourceToDestination
</verification>

<success_criteria>
- 4 VolumeProfile assets created with distinct per-hero post-processing values
- HeroThemeConfig SOs updated with non-null volumeProfile references
- Existing VolumeProfileTransitioner code works unchanged (it already reads theme.volumeProfile)
</success_criteria>

<output>
After completion, create `.planning/phases/06-character-select-aaa-rebuild/06-01-SUMMARY.md`
</output>
