---
phase: 03-controller-behavior
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/UI/Screens/CharacterSelect.uxml
  - Assets/UI/Styles/CharacterSelect.uss
  - Assets/Scripts/UI/CharacterSelect/CharSelectEvents.cs
autonomous: true
requirements: [CTRL-01, CTRL-09]

must_haves:
  truths:
    - "UXML has rule-of-thirds layout: hero-stage 50% left, info-panel-container right half with tab-header-strip and tab-content"
    - "Confirm overlay, btn-confirm, and btn-cancel are completely removed from UXML"
    - "Embark area is positioned bottom-right of info area with embark-progress-ring element for hold-to-confirm"
    - "Toast container exists for error notifications (hidden by default)"
    - "Tabbed info has three content sections: tab-overview-content, tab-abilities-content, tab-lore-content"
    - "USS has veil-torn dark fantasy panel styles with obsidian/iron borders and per-hero accent glow via theme classes"
    - "CharSelectEvents has new events: OnTabChanged, OnEmbarkHoldProgress, OnErrorOccurred, OnLoadingStarted, OnLoadingComplete"
  artifacts:
    - path: "Assets/UI/Screens/CharacterSelect.uxml"
      provides: "Rule-of-thirds layout with tabbed info, hold-to-embark area, toast container"
      contains: "info-panel-container"
    - path: "Assets/UI/Styles/CharacterSelect.uss"
      provides: "Veil-torn dark fantasy panel styles, tab system styles, skeleton shimmer, focus ring, toast notification styles"
      contains: ".veil-panel"
    - path: "Assets/Scripts/UI/CharacterSelect/CharSelectEvents.cs"
      provides: "Extended event bus with tab, embark hold, error, and loading events"
      exports: ["OnTabChanged", "OnEmbarkHoldProgress", "OnErrorOccurred", "OnLoadingStarted", "OnLoadingComplete"]
  key_links:
    - from: "Assets/UI/Screens/CharacterSelect.uxml"
      to: "Assets/UI/Styles/CharacterSelect.uss"
      via: "USS class references in UXML elements"
      pattern: "class=\"veil-panel"
---

<objective>
Restructure the UXML from symmetric dual-panel to rule-of-thirds composition with tabbed info system, remove the confirm overlay (replaced by hold-to-embark in Plan 02), add veil-torn dark fantasy USS styling, and extend CharSelectEvents with new events needed by all subsequent plans.

Purpose: This is the structural foundation for all Phase 3 behavior. Every other plan depends on the new UXML element names, USS classes, and event definitions created here. The layout shift from symmetric to rule-of-thirds is the single biggest visual upgrade per user's vision.

Output: Restructured UXML, comprehensive USS, extended CharSelectEvents.cs
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-controller-behavior/03-CONTEXT.md
@.planning/phases/03-controller-behavior/03-RESEARCH.md
@.planning/phases/02-layout-structure/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure UXML to Rule-of-Thirds with Tabbed Info and Remove Confirm Overlay</name>
  <read_first>
    - Assets/UI/Screens/CharacterSelect.uxml
    - Assets/Scripts/UI/CharacterSelect/CharacterSelectManager.cs
    - .planning/phases/03-controller-behavior/03-CONTEXT.md
    - .planning/phases/03-controller-behavior/03-RESEARCH.md
  </read_first>
  <files>
    Assets/UI/Screens/CharacterSelect.uxml
  </files>
  <action>
Rewrite CharacterSelect.uxml with the following structure (preserve the Style src reference to CharacterSelect.uss):

```xml
character-select-root (class: character-select-root theme-vex)
  parallax-bg/                         (KEEP existing parallax layers exactly as-is)
    parallax-deep/ > void-stars
    parallax-fog/ > fog-particles
    background-vignette

  hero-stage (class: hero-stage)       (50% left half, full height)
    hero-aura-container/ > aura-base
    hero-render-target (class: hero-render-target)
    drag-hint (Label: "DRAG TO ROTATE")

  info-panel-container (class: veil-panel info-panel-container)  (right half)
    tab-header-strip (class: tab-header-strip)
      tab-btn-overview (Button, class: tab-btn tab-btn-active, text: "OVERVIEW")
      tab-btn-abilities (Button, class: tab-btn, text: "ABILITIES")
      tab-btn-lore (Button, class: tab-btn, text: "LORE")
    tab-content (class: tab-content)
      tab-overview-content (class: tab-content-section tab-active)
        hero-name (Label, class: hero-name)
        hero-title (Label, class: hero-title)
        hero-quote (Label, class: hero-quote)
        hero-class-info (class: hero-class-info)
          path-row (class: info-row) > info-label "PATH" + hero-path (info-value)
          role-row (class: info-row) > info-label "ROLE" + hero-role (info-value)
          synergy-row (class: info-row) > info-label "SYNERGY" + hero-synergy (info-value)
        starter-stats-grid (class: starter-stats-grid)
          4x stat-chip: HP/ATK/DEF/SPD with stat-chip-label + stat-chip-value (names: stat-hp, stat-atk, stat-def, stat-spd)
        champion-section (class: champion-section)
          section-header "CHAMPION"
          champion-name (Label)
          champion-tags > champion-brand (tag) + champion-role (tag)
      tab-abilities-content (class: tab-content-section)
        abilities-header (Label, class: section-header, text: "ABILITIES")
        abilities-list (class: abilities-list)
          ability-0 through ability-4 (Label, class: ability-slot)
      tab-lore-content (class: tab-content-section)
        lore-header (Label, class: section-header, text: "LORE")
        hero-lore-section (class: hero-lore-section)
          hero-backstory (Label, class: hero-backstory)
        synergy-detail-section (class: synergy-detail-section)
          synergy-detail-header (Label, class: section-header, text: "SYNERGY")
          hero-synergy-detail (Label, class: hero-synergy-detail)
        brands-section (class: brands-section)
          brands-header (Label, class: section-header, text: "BRANDS")
          hero-brands-detail (Label, class: hero-brands-detail)

  embark-area (class: embark-area)     (bottom-right of info area)
    btn-embark (Button, class: btn-embark)
      embark-hex-bg (class: embark-hex-bg)
      embark-glow (class: embark-glow)
      embark-text (Label, class: embark-text, text: "EMBARK AS VEX")
      embark-subtitle (Label, class: embark-subtitle, text: "HOLD TO EMBARK")
    embark-progress-ring (class: embark-progress-ring)  (NEW: visual for hold-to-confirm)

  btn-back (Button, class: btn-back, text: "< BACK")  (top-left)

  bottom-layer (class: bottom-layer)   (carousel at screen bottom)
    btn-prev (Button, class: nav-arrow nav-arrow-left)
    carousel-container > carousel-strip
    btn-next (Button, class: nav-arrow nav-arrow-right)
    hero-index (Label)

  cinematic-overlays (class: cinematic-overlays)  (KEEP as-is)
    scanlines, vignette-outer, veil-glow

  toast-container (class: toast-container toast-hidden)  (NEW: error notifications)
    toast-content (class: toast-content)
      toast-icon (class: toast-icon)
      toast-message (Label, class: toast-message, text: "")
    toast-actions (class: toast-actions)
      btn-toast-retry (Button, class: toast-btn, text: "RETRY")
      btn-toast-back (Button, class: toast-btn, text: "BACK TO MENU")

  skeleton-overlay (class: skeleton-overlay hidden)  (NEW: loading state overlay)
```

CRITICAL removals (per user decision):
- DELETE the entire confirm-overlay VisualElement and all children (confirm-popup, btn-confirm, btn-cancel, confirm-title, confirm-description, confirm-buttons)
- Change embark-subtitle text from "Begin Your Journey" to "HOLD TO EMBARK"

CRITICAL additions:
- info-panel-container with tab-header-strip and three tab-content-section children
- embark-progress-ring inside embark-area
- toast-container with message, retry button, and back button
- skeleton-overlay for loading states

Preserve all existing element names that are referenced by C# controllers (hero-name, hero-title, hero-quote, hero-path, hero-role, hero-synergy, stat-hp/atk/def/spd, champion-name/brand/role, hero-backstory, hero-render-target, carousel-strip, hero-index, btn-prev, btn-next, btn-back, btn-embark, embark-text, embark-glow, embark-hex-bg, parallax-deep, parallax-fog, fog-particles, background-vignette, hero-aura-container, aura-base, drag-hint, scanlines, vignette-outer, veil-glow).

The stats-panel (right panel with attributes/bars) is REMOVED as a separate panel. Its content (attribute bars and abilities) is now split: abilities go into tab-abilities-content. The D&D attribute bars (STR/DEX/CON/INT/WIS/CHA) are kept but moved into a new section inside tab-overview-content, placed after starter-stats-grid:

```xml
attribute-bars-section (class: attribute-bars-section)
  attribute-bars-header (Label, class: section-header, text: "ATTRIBUTES")
  attribute-bars (class: attribute-bars)
    (6x stat-bar-row with bar-str/dex/con/int/wis/cha-fill and bar-str/dex/con/int/wis/cha-value -- same structure as current)
```
  </action>
  <verify>
    1. grep for "confirm-overlay" in CharacterSelect.uxml -- must return 0 matches
    2. grep for "btn-confirm" in CharacterSelect.uxml -- must return 0 matches
    3. grep for "btn-cancel" in CharacterSelect.uxml -- must return 0 matches
    4. grep for "info-panel-container" in CharacterSelect.uxml -- must return 1+ matches
    5. grep for "tab-header-strip" in CharacterSelect.uxml -- must return 1+ matches
    6. grep for "tab-overview-content" in CharacterSelect.uxml -- must return 1+ matches
    7. grep for "tab-abilities-content" in CharacterSelect.uxml -- must return 1+ matches
    8. grep for "tab-lore-content" in CharacterSelect.uxml -- must return 1+ matches
    9. grep for "embark-progress-ring" in CharacterSelect.uxml -- must return 1+ matches
    10. grep for "toast-container" in CharacterSelect.uxml -- must return 1+ matches
    11. grep for "HOLD TO EMBARK" in CharacterSelect.uxml -- must return 1+ matches
    12. grep for "hero-render-target" in CharacterSelect.uxml -- must return 1+ match (preserved)
    13. grep for "carousel-strip" in CharacterSelect.uxml -- must return 1+ match (preserved)
  </verify>
  <acceptance_criteria>
    - confirm-overlay, btn-confirm, btn-cancel are absent from CharacterSelect.uxml
    - info-panel-container contains tab-header-strip with 3 tab buttons and tab-content with 3 sections
    - tab-overview-content contains hero-name, hero-title, hero-quote, hero-class-info, starter-stats-grid, champion-section, attribute-bars
    - tab-abilities-content contains abilities-list with ability-0 through ability-4
    - tab-lore-content contains hero-backstory, hero-synergy-detail, hero-brands-detail
    - embark-area contains btn-embark and embark-progress-ring
    - toast-container with toast-message label, btn-toast-retry, btn-toast-back exists
    - skeleton-overlay element exists with class "hidden"
    - All previously-referenced element names (hero-name, stat-hp, carousel-strip, etc.) still exist in the file
  </acceptance_criteria>
  <done>UXML restructured to rule-of-thirds with tabbed info panel, confirm overlay removed, hold-to-embark progress ring added, toast and skeleton overlay elements present</done>
</task>

<task type="auto">
  <name>Task 2: Create Veil-Torn Dark Fantasy USS with Tab System, Focus Ring, Skeleton Shimmer, and Toast Styles</name>
  <read_first>
    - Assets/UI/Styles/CharacterSelect.uss
    - Assets/UI/Screens/CharacterSelect.uxml (the newly restructured version from Task 1)
    - Assets/UI/Styles/VeilBreakers.uss
    - .planning/phases/03-controller-behavior/03-CONTEXT.md
  </read_first>
  <files>
    Assets/UI/Styles/CharacterSelect.uss
  </files>
  <action>
Rewrite CharacterSelect.uss to implement the veil-torn dark fantasy aesthetic. This is a comprehensive USS rewrite. Key sections:

**1. Root and Layout:**
- `.character-select-root`: position: absolute; width: 100%; height: 100%; background-color: rgb(8, 6, 12);
- `.hero-stage`: position: absolute; left: 0; top: 0; width: 50%; height: 100%; (left half for 3D hero)
- `.info-panel-container`: position: absolute; right: 0; top: 60px; width: 46%; height: calc(100% - 180px); (right half for tabbed info, leave room for embark area bottom and back button top)

**2. Veil-Torn Panel Material (.veil-panel):**
Replace glass-panel with veil-panel. The veil-panel class:
- background-color: rgba(12, 10, 18, 0.92);
- border-width: 2px;
- border-color: rgba(60, 50, 70, 0.8);
- border-radius: 6px;
- padding: 16px;
- overflow: hidden;
Per-hero accent glow on borders via theme classes:
- `.theme-vex .veil-panel`: border-color: rgba(200, 160, 60, 0.6);
- `.theme-seraphina .veil-panel`: border-color: rgba(140, 80, 200, 0.6);
- `.theme-orion .veil-panel`: border-color: rgba(180, 50, 50, 0.6);
- `.theme-nyx .veil-panel`: border-color: rgba(60, 180, 200, 0.6);
Theme accent glow transitions: transition-property: border-color; transition-duration: 0.4s; transition-timing-function: ease-in-out;

**3. Tab System:**
- `.tab-header-strip`: flex-direction: row; justify-content: flex-start; padding-bottom: 8px; border-bottom-width: 1px; border-bottom-color: rgba(60, 50, 70, 0.5);
- `.tab-btn`: background-color: transparent; color: rgba(180, 170, 190, 0.5); border-width: 0; border-bottom-width: 2px; border-bottom-color: transparent; padding: 8px 16px; font-size: 13px; -unity-font-style: bold; letter-spacing: 2px; transition-property: color, border-bottom-color, opacity; transition-duration: 0.2s, 0.2s, 0.2s;
- `.tab-btn-active`: color: rgba(240, 230, 250, 1); border-bottom-color: rgba(200, 160, 60, 0.9);
- `.tab-btn:hover`: color: rgba(220, 210, 230, 0.8);
- `.tab-content`: flex-grow: 1; overflow: hidden;
- `.tab-content-section`: display: none; flex-grow: 1; padding: 12px 0;
- `.tab-content-section.tab-active`: display: flex;

**4. Focus Ring (gamepad navigation):**
- `.focusable-zone`: border-width: 2px; border-color: rgba(0, 0, 0, 0); border-radius: 4px; transition-property: border-color, scale; transition-duration: 0.15s, 0.15s; transition-timing-function: ease-out, ease-out;
- `.focusable-zone:focus`: border-color: rgba(255, 180, 60, 0.9); scale: 1.02 1.02;
- `.focus-ring-active`: border-color: rgba(255, 180, 60, 0.9); scale: 1.02 1.02;
Per-hero focus ring colors via theme:
- `.theme-vex .focus-ring-active`: border-color: rgba(200, 160, 60, 0.9);
- `.theme-seraphina .focus-ring-active`: border-color: rgba(140, 80, 200, 0.9);
- `.theme-orion .focus-ring-active`: border-color: rgba(180, 50, 50, 0.9);
- `.theme-nyx .focus-ring-active`: border-color: rgba(60, 180, 200, 0.9);

**5. Embark Area (hold-to-confirm):**
- `.embark-area`: position: absolute; bottom: 20px; right: 2%; width: 42%; align-items: flex-end;
- `.embark-progress-ring`: position: absolute; width: 64px; height: 64px; border-radius: 32px; border-width: 3px; border-color: rgba(255, 180, 60, 0); transition-property: border-color, opacity; transition-duration: 0.2s, 0.2s;
- `.embark-progress-ring.hold-active`: border-color: rgba(255, 180, 60, 0.9); opacity: 1;
Keep existing btn-embark, embark-hex-bg, embark-glow, embark-text, embark-subtitle styles but update to match dark fantasy (darker backgrounds, gold/amber accents).

**6. Toast Notification:**
- `.toast-container`: position: absolute; bottom: -100px; left: 50%; translate: -50% 0; width: 400px; transition-property: translate; transition-duration: 0.4s; transition-timing-function: ease-out;
- `.toast-container.toast-visible`: translate: -50% -120px;
- `.toast-container.toast-hidden`: translate: -50% 0;
- `.toast-content`: flex-direction: row; background-color: rgba(40, 15, 15, 0.95); border-width: 1px; border-color: rgba(200, 60, 60, 0.6); border-radius: 6px; padding: 12px 16px; align-items: center;
- `.toast-message`: color: rgba(240, 200, 200, 0.9); font-size: 13px; flex-grow: 1;
- `.toast-actions`: flex-direction: row; padding-top: 8px; justify-content: flex-end;
- `.toast-btn`: background-color: rgba(60, 20, 20, 0.8); color: rgba(240, 200, 200, 0.9); border-width: 1px; border-color: rgba(200, 60, 60, 0.4); border-radius: 4px; padding: 6px 14px; margin-left: 8px; font-size: 12px;

**7. Skeleton Shimmer:**
- `.skeleton-overlay`: position: absolute; left: 0; top: 0; right: 0; bottom: 0; background-color: rgba(12, 10, 18, 0.7);
- `.skeleton-shimmer`: background-color: rgba(40, 40, 50, 0.6); border-radius: 4px; min-height: 16px; margin: 4px 0;
- `.shimmer-bar`: position: absolute; width: 60px; height: 100%; background-color: rgba(255, 255, 255, 0.06); rotate: 15deg; translate: -60px 0; transition-property: translate; transition-duration: 1.2s; transition-timing-function: ease-in-out;
- `.shimmer-bar.shimmer-active`: translate: 400px 0;

**8. Existing Element Styles (update to dark fantasy):**
Preserve all existing class names that are referenced by C# code but update colors:
- `.hero-name`: font-size: 32px; color: rgba(240, 230, 250, 1); -unity-font-style: bold; letter-spacing: 3px;
- `.hero-title`: font-size: 16px; color: rgba(180, 170, 190, 0.7); letter-spacing: 2px;
- `.hero-quote`: font-size: 13px; color: rgba(160, 150, 170, 0.6); -unity-font-style: italic;
- `.info-row`: flex-direction: row; margin: 2px 0; align-items: flex-start;
- `.info-label`: width: 80px; font-size: 11px; color: rgba(140, 130, 150, 0.6); letter-spacing: 2px;
- `.info-value`: flex-grow: 1; font-size: 13px; color: rgba(220, 210, 230, 0.9); white-space: normal; overflow: hidden; flex-shrink: 1;
- Keep stat-chip, stat-bar-row, stat-bar-track, stat-bar-fill, stat-bar-value styles updated to dark fantasy palette
- Keep champion-section, tag, ability-slot styles updated
- Keep hero-card, carousel-strip, bottom-layer, nav-arrow styles updated
- Keep parallax, cinematic-overlay styles as-is

**9. Panel transition classes:**
- `.panel-hidden`: opacity: 0; translate: -20px 0;
- Default panel state (no .panel-hidden): opacity: 1; translate: 0 0; with transition-property: opacity, translate; transition-duration: 0.3s, 0.3s;

**10. Hidden utility:**
- `.hidden`: display: none;

CRITICAL: All transitions must use ONLY GPU-safe properties: translate, scale, rotate, opacity, color, border-color. NO width, height, margin, left, top, padding transitions.
  </action>
  <verify>
    1. grep for "transition.*width" in CharacterSelect.uss -- must return 0 matches (no width transitions)
    2. grep for "transition.*height" in CharacterSelect.uss -- must return 0 matches
    3. grep for "transition.*margin" in CharacterSelect.uss -- must return 0 matches
    4. grep for ".veil-panel" in CharacterSelect.uss -- must return 1+ matches
    5. grep for ".tab-btn" in CharacterSelect.uss -- must return 1+ matches
    6. grep for ".tab-content-section" in CharacterSelect.uss -- must return 1+ matches
    7. grep for ".focus-ring-active" in CharacterSelect.uss -- must return 1+ matches
    8. grep for ".toast-container" in CharacterSelect.uss -- must return 1+ matches
    9. grep for ".skeleton-shimmer" in CharacterSelect.uss -- must return 1+ matches
    10. grep for ".embark-progress-ring" in CharacterSelect.uss -- must return 1+ matches
    11. grep for ".theme-vex .veil-panel" in CharacterSelect.uss -- must return 1+ matches
    12. grep for ".theme-nyx .veil-panel" in CharacterSelect.uss -- must return 1+ matches
    13. grep for "glass-panel" in CharacterSelect.uss -- should be minimal/removed (replaced by veil-panel)
  </verify>
  <acceptance_criteria>
    - .veil-panel class exists with dark fantasy colors (background-color containing rgba(12, 10, 18)
    - 4 theme-specific border-color overrides exist (.theme-vex, .theme-seraphina, .theme-orion, .theme-nyx on .veil-panel)
    - .tab-btn, .tab-btn-active, .tab-content-section, .tab-content-section.tab-active classes exist
    - .focusable-zone and .focus-ring-active classes exist with border-color transitions
    - .toast-container, .toast-visible, .toast-hidden classes exist with translate transitions
    - .skeleton-shimmer and .shimmer-bar classes exist
    - .embark-progress-ring class exists
    - .panel-hidden class exists with opacity: 0 and translate
    - Zero transitions on width, height, or margin properties
    - .hidden class exists with display: none
    - All theme-specific focus ring colors defined (4 heroes)
  </acceptance_criteria>
  <done>USS completely rewritten with veil-torn dark fantasy aesthetic, tab system, focus ring, toast, skeleton shimmer, and hold-to-embark progress styles. All transitions GPU-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Extend CharSelectEvents with Tab, Hold, Error, and Loading Events</name>
  <read_first>
    - Assets/Scripts/UI/CharacterSelect/CharSelectEvents.cs
    - .planning/phases/03-controller-behavior/03-RESEARCH.md
  </read_first>
  <files>
    Assets/Scripts/UI/CharacterSelect/CharSelectEvents.cs
  </files>
  <action>
Add the following new events and raise methods to CharSelectEvents.cs, preserving all existing events and the ClearAll() method pattern:

New static events to add (after existing events, before INVOCATION HELPERS section):
```csharp
/// <summary>Tab index changed (0=Overview, 1=Abilities, 2=Lore).</summary>
public static event Action<int> OnTabChanged;

/// <summary>Embark hold progress updated (0..1). 0 = released, 1 = complete.</summary>
public static event Action<float> OnEmbarkHoldProgress;

/// <summary>Error occurred that should display a toast notification.</summary>
public static event Action<string> OnErrorOccurred;

/// <summary>Async loading started -- show skeleton shimmer.</summary>
public static event Action OnLoadingStarted;

/// <summary>Async loading complete -- hide skeleton shimmer.</summary>
public static event Action OnLoadingComplete;

/// <summary>Focus zone changed for gamepad navigation.</summary>
public static event Action<int> OnFocusZoneChanged;
```

New raise methods to add (in INVOCATION HELPERS section):
```csharp
public static void RaiseTabChanged(int tabIndex) => OnTabChanged?.Invoke(tabIndex);
public static void RaiseEmbarkHoldProgress(float progress) => OnEmbarkHoldProgress?.Invoke(progress);
public static void RaiseErrorOccurred(string message) => OnErrorOccurred?.Invoke(message);
public static void RaiseLoadingStarted() => OnLoadingStarted?.Invoke();
public static void RaiseLoadingComplete() => OnLoadingComplete?.Invoke();
public static void RaiseFocusZoneChanged(int zoneIndex) => OnFocusZoneChanged?.Invoke(zoneIndex);
```

Update ClearAll() to null all new events:
```csharp
OnTabChanged = null;
OnEmbarkHoldProgress = null;
OnErrorOccurred = null;
OnLoadingStarted = null;
OnLoadingComplete = null;
OnFocusZoneChanged = null;
```

Also remove the existing `OnEmbarkRequested`, `OnEmbarkConfirmed`, `OnEmbarkCancelled` events and their Raise methods since the confirm popup flow is being removed. Replace with:
```csharp
/// <summary>Embark hold completed -- begin async embark sequence.</summary>
public static event Action OnEmbarkTriggered;
public static void RaiseEmbarkTriggered() => OnEmbarkTriggered?.Invoke();
```
And null `OnEmbarkTriggered` in ClearAll().
  </action>
  <verify>
    1. grep for "OnTabChanged" in CharSelectEvents.cs -- must return 2+ matches (declaration + raise)
    2. grep for "OnEmbarkHoldProgress" in CharSelectEvents.cs -- must return 2+ matches
    3. grep for "OnErrorOccurred" in CharSelectEvents.cs -- must return 2+ matches
    4. grep for "OnLoadingStarted" in CharSelectEvents.cs -- must return 2+ matches
    5. grep for "OnLoadingComplete" in CharSelectEvents.cs -- must return 2+ matches
    6. grep for "OnFocusZoneChanged" in CharSelectEvents.cs -- must return 2+ matches
    7. grep for "OnEmbarkTriggered" in CharSelectEvents.cs -- must return 2+ matches
    8. grep for "OnEmbarkRequested" in CharSelectEvents.cs -- must return 0 matches (removed)
    9. grep for "OnEmbarkConfirmed" in CharSelectEvents.cs -- must return 0 matches (removed)
    10. grep for "OnEmbarkCancelled" in CharSelectEvents.cs -- must return 0 matches (removed)
    11. grep for "OnTabChanged = null" in CharSelectEvents.cs -- must return 1 match (in ClearAll)
    12. grep for "OnEmbarkTriggered = null" in CharSelectEvents.cs -- must return 1 match (in ClearAll)
  </verify>
  <acceptance_criteria>
    - OnTabChanged, OnEmbarkHoldProgress, OnErrorOccurred, OnLoadingStarted, OnLoadingComplete, OnFocusZoneChanged events declared with XML doc comments
    - Corresponding RaiseTabChanged, RaiseEmbarkHoldProgress, RaiseErrorOccurred, RaiseLoadingStarted, RaiseLoadingComplete, RaiseFocusZoneChanged methods exist
    - OnEmbarkTriggered replaces the removed OnEmbarkRequested/Confirmed/Cancelled events
    - ClearAll() nulls all new events plus OnEmbarkTriggered
    - OnEmbarkRequested, OnEmbarkConfirmed, OnEmbarkCancelled are completely removed
    - All existing events (OnHeroChanged, OnScreenReady, OnScreenExiting, OnNavigationRequested) are preserved
    - File compiles (namespace VeilBreakers.UI.CharacterSelect, using System + VeilBreakers.Data)
  </acceptance_criteria>
  <done>CharSelectEvents extended with 6 new events (tab, embark hold, error, loading, focus zone) and old confirm popup events replaced with OnEmbarkTriggered</done>
</task>

</tasks>

<verification>
1. CharacterSelect.uxml has rule-of-thirds layout (hero-stage left, info-panel-container right)
2. No confirm-overlay, btn-confirm, or btn-cancel in UXML
3. Tab system (3 tabs: Overview, Abilities, Lore) present in UXML
4. CharacterSelect.uss has veil-panel, tab, focus ring, toast, shimmer styles
5. All USS transitions use only GPU-safe properties
6. CharSelectEvents.cs has all new events and ClearAll nulls them
7. All previously-referenced element names preserved in UXML
</verification>

<success_criteria>
- UXML restructured from symmetric dual-panel to rule-of-thirds with tabbed info system
- Confirm overlay completely removed, replaced by hold-to-embark progress ring element
- USS implements veil-torn dark fantasy aesthetic with per-hero accent colors
- Tab system fully styled with active/inactive states
- Focus ring, toast notification, skeleton shimmer styles complete
- CharSelectEvents extended with all events needed by Plans 02 and 03
- Zero non-GPU-safe transitions in USS
</success_criteria>

<output>
After completion, create `.planning/phases/03-controller-behavior/03-01-SUMMARY.md`
</output>
