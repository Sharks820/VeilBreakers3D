---
phase: 02-layout-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/UI/Screens/CharacterSelect.uxml
  - Assets/UI/Styles/CharacterSelect.uss
  - Assets/Scripts/UI/CharacterSelect/CharacterSelectManager.cs
  - Assets/Scripts/UI/CharacterSelect/HeroDataPanelController.cs
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04
  - LAYOUT-05

must_haves:
  truths:
    - "Hero info panel displays name, title, quote, path, and role for all 4 heroes without text overlapping or clipping at 1920x1080"
    - "Champion/starter monster area shows monster name, brand icon, and role tag correctly for all 4 heroes"
    - "Hero backstory text populates correctly for each hero when navigating through all 4 characters"
    - "No panel content overflows or clips at 1920x1080"
    - "EnsureFullScreenLayout() hack is deleted and layout works natively via USS"
  artifacts:
    - path: "Assets/UI/Screens/CharacterSelect.uxml"
      provides: "Backstory/lore section in hero-info-panel"
      contains: "hero-backstory"
    - path: "Assets/UI/Styles/CharacterSelect.uss"
      provides: "Text overflow prevention styles and backstory section styles"
      contains: "hero-lore-section"
    - path: "Assets/Scripts/UI/CharacterSelect/CharacterSelectManager.cs"
      provides: "EnsureFullScreenLayout removed, layout handled by USS"
    - path: "Assets/Scripts/UI/CharacterSelect/HeroDataPanelController.cs"
      provides: "Backstory population and champion display fixes"
      contains: "backstory"
  key_links:
    - from: "Assets/Scripts/UI/CharacterSelect/HeroDataPanelController.cs"
      to: "Assets/UI/Screens/CharacterSelect.uxml"
      via: "Q() query for hero-backstory element"
      pattern: "hero-backstory"
    - from: "Assets/UI/Styles/CharacterSelect.uss"
      to: "Assets/UI/Screens/CharacterSelect.uxml"
      via: "USS class selectors applied to UXML elements"
      pattern: "hero-lore-section|hero-backstory"
    - from: "Assets/Scripts/UI/CharacterSelect/CharacterSelectManager.cs"
      to: "Assets/UI/Styles/CharacterSelect.uss"
      via: "TemplateContainer flex-grow rule replaces C# hack"
      pattern: "TemplateContainer"
---

<objective>
Fix UXML structure, content population, and panel sizing for pixel-correct layout at 1920x1080.

Purpose: The Character Select screen must display all hero information (name, title, quote, path, role, backstory, champion data) without text overlap or clipping. The EnsureFullScreenLayout() C# hack must be replaced by proper USS-driven layout so the UI framework handles sizing declaratively.

Output: Updated UXML with backstory section, USS with text overflow fixes, C# with EnsureFullScreenLayout removed and backstory population added.
</objective>

<execution_context>
@C:/Users/Conner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Conner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-structure/02-RESEARCH.md
@Assets/UI/Screens/CharacterSelect.uxml
@Assets/UI/Styles/CharacterSelect.uss
@Assets/Scripts/UI/CharacterSelect/CharacterSelectManager.cs
@Assets/Scripts/UI/CharacterSelect/HeroDataPanelController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UXML Root Layout and Add Backstory Section</name>
  <files>
    Assets/UI/Screens/CharacterSelect.uxml
    Assets/UI/Styles/CharacterSelect.uss
    Assets/Scripts/UI/CharacterSelect/CharacterSelectManager.cs
  </files>
  <action>
    **UXML Changes (CharacterSelect.uxml):**
    1. Add a hero-lore-section inside hero-info-panel, AFTER the champion-section. Structure:
       ```xml
       <ui:VisualElement name="hero-lore-section" class="hero-lore-section">
           <ui:Label class="section-header" text="LORE" />
           <ui:Label name="hero-backstory" class="hero-backstory" text="" />
       </ui:VisualElement>
       ```
    2. Verify the synergy label (`hero-synergy` or equivalent) has proper structure for long text wrapping.

    **USS Changes (CharacterSelect.uss):**
    1. Add `.hero-lore-section` styles:
       - `margin-top: 16px; padding-top: 12px;`
       - `border-top-width: 1px; border-top-color: rgba(255, 255, 255, 0.08);`
       - `max-height: 120px; overflow: hidden;`
    2. Add `.hero-backstory` styles:
       - `font-size: 10px; color: rgba(200, 200, 215, 0.6);`
       - `white-space: normal; -unity-font-style: italic;`
    3. Fix text overflow on `.info-value` or equivalent synergy text labels:
       - Add `white-space: normal; overflow: hidden;` to prevent long synergy_explanation text from breaking panel layout.
    4. Ensure `.hero-info-panel` has `overflow: hidden;` to contain all child content within the panel bounds.
    5. DO NOT touch any `transition:` declarations in this task -- that is Plan 02's concern.

    **C# Changes (CharacterSelectManager.cs):**
    1. Find and DELETE the `EnsureFullScreenLayout()` method entirely.
    2. Remove ALL calls to `EnsureFullScreenLayout()` (likely in `OnEnable`, `Start`, or initialization flow).
    3. The USS rules `TemplateContainer { flex-grow: 1; }` (already exist in both VeilBreakers.uss line 16-18 and CharacterSelect.uss line 81-83) handle full-screen layout declaratively. Also verify `.character-select-root` has `position: absolute; width: 100%; height: 100%;` in USS.
    4. If after removal the layout collapses on fresh scene load, check that the PanelSettings asset has VeilBreakers.uss (or VeilBreakersTheme.tss) set as the default stylesheet. Do NOT re-add the hack -- fix the USS root cause instead.

    **Important:** Follow VeilBreakers conventions. Use `_` prefix for private fields, `k` prefix for string constants. Add Debug.Assert for any new Q() queries.
  </action>
  <verify>
    1. Open Unity project -- verify zero compilation errors.
    2. Grep for `EnsureFullScreenLayout` in all .cs files -- must return zero results.
    3. Grep for `hero-backstory` in CharacterSelect.uxml -- must find the new element.
    4. Grep for `hero-lore-section` in CharacterSelect.uss -- must find the new styles.
    5. Verify CharacterSelect.uss contains `overflow: hidden` on hero-info-panel or its container.
  </verify>
  <done>
    EnsureFullScreenLayout() is fully deleted from CharacterSelectManager.cs. CharacterSelect.uxml contains a hero-lore-section with hero-backstory label inside hero-info-panel. CharacterSelect.uss has text overflow prevention styles. The layout renders at full screen via USS rules alone, not C# hacks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Content Population for Backstory, Champion Display, and Text Overlap</name>
  <files>
    Assets/Scripts/UI/CharacterSelect/HeroDataPanelController.cs
  </files>
  <action>
    **HeroDataPanelController.cs Changes:**

    1. **Add backstory element caching (LAYOUT-03):**
       - Add a private field: `private Label _heroBackstory;`
       - Add a constant: `private const string kHeroBackstory = "hero-backstory";`
       - In `CacheReferences()` (or equivalent init method), query and cache: `_heroBackstory = root.Q<Label>(kHeroBackstory);`
       - Add `Debug.Assert(_heroBackstory != null, $"[HeroDataPanel] Missing element: {kHeroBackstory}");`

    2. **Populate backstory on hero switch (LAYOUT-03):**
       - In the method that populates hero data (likely `PopulateHeroInfo()` or `UpdateHeroData()`), add:
         ```csharp
         if (_heroBackstory != null)
             _heroBackstory.text = heroData.backstory ?? string.Empty;
         ```
       - The `HeroData.backstory` field exists in the JSON data model (200-400 chars per hero).

    3. **Verify champion display (LAYOUT-02):**
       - Confirm `PopulateChampion()` correctly uses `GetPrimaryBrand()` for brand display and `GetAIPattern()` for role display (per Phase 1 fix in 01-03-SUMMARY: "H3: Champion role uses GetAIPattern()").
       - Verify the champion name, brand name, and role tag are all populated. If any are missing, add the population logic.
       - Ensure brand icon/tag USS classes render correctly (verify `.tag` or `.champion-section` styles exist in USS).

    4. **Verify text overlap prevention (LAYOUT-01):**
       - Ensure the synergy explanation text label has appropriate text wrapping. The synergy_explanation can be 130+ characters (Seraphina's is the longest at 131 chars). The USS fix in Task 1 adds `white-space: normal`, but verify the C# code sets the text correctly without additional formatting that might break layout.
       - If the synergy text is being uppercased via C#, verify the uppercased version still fits within the panel. Consider removing the uppercase transform if it causes overflow.

    5. **Verify all 4 heroes populate correctly (LAYOUT-04):**
       - Confirm that navigating through Vex, Seraphina, Orion, and Nyx populates: name, title, quote, path, role, synergy text, backstory, champion name, champion brand, and champion role.
       - Each data field should have a null-coalesce fallback (`?? string.Empty` or `?? "Unknown"`) to prevent NRE.

    **Conventions:** Use `_` prefix for private fields, `k` prefix for element name constants. Add `Debug.Assert` for all new Q() queries. Keep method size under 30 lines.
  </action>
  <verify>
    1. Unity compiles with zero errors.
    2. Grep for `_heroBackstory` in HeroDataPanelController.cs -- must exist as a cached field.
    3. Grep for `kHeroBackstory` -- must exist as a named constant.
    4. Grep for `Debug.Assert.*kHeroBackstory` -- must have validation.
    5. Grep for `backstory` in the populate method -- must set the text.
  </verify>
  <done>
    HeroDataPanelController populates backstory text for all 4 heroes. Champion display shows name, brand, and role correctly. All text fields have null-safe fallbacks. All 4 heroes display complete information without text overlap at 1920x1080.
  </done>
</task>

</tasks>

<verification>
1. Zero compilation errors in Unity.
2. `EnsureFullScreenLayout` does not appear in any .cs file.
3. CharacterSelect.uxml contains hero-lore-section with hero-backstory label.
4. CharacterSelect.uss has overflow prevention on hero-info-panel and text labels.
5. HeroDataPanelController.cs populates backstory, champion name/brand/role for all heroes.
6. All new Q() queries have Debug.Assert validation and kConstant names.
</verification>

<success_criteria>
- Hero info panel renders name, title, quote, path, role, synergy, and backstory for all 4 heroes without overlap or clipping at 1920x1080
- Champion section shows monster name, brand, and role tag for all 4 heroes
- EnsureFullScreenLayout() is completely removed; layout works via USS alone
- No panel overflow at 1920x1080
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-structure/02-01-SUMMARY.md`
</output>
