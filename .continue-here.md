---
phase: aaa-ui-overhaul
task: V2-1 (C# visual pass)
total_tasks: 8 remaining
status: paused_before_v2
last_updated: 2026-03-24T00:33:36Z
---

<current_state>
AAA UI overhaul on feature/aaa-ui-overhaul branch. 17 USS/UXML tasks completed and committed (0 compile errors). BUT the visual result looks terrible because USS alone cannot achieve AAA quality — it lacks gradients, box-shadow, blur. Need a COMPLETE C# visual pass to match the approved mockup.

The user is VERY frustrated. The mockup looked amazing, the implementation looks like garbage. DO NOT repeat this mistake. The C# route is mandatory.
</current_state>

<completed_work>
STRUCTURAL CHANGES (all done, all correct):
- Vertical button layout (right-aligned)
- All buttons same size 300x56px, dark base, orange on hover
- Logo dark backing for visibility
- SPD → STAMINA + dynamic MANA for casters (VOIDTOUCHED/UNCHAINED)
- Individual stat colors: HP=red, STAMINA=green, ATK=orange, DEF=blue
- D&D attributes as numbers (7: STR/DEX/CON/INT/WIS/CHA/SPD)
- Champion 50/50 split (model left, info right, brand highlight)
- Panel depth USS (border-top hero color, layered bg)
- Embark gold restyle + visible HOLD TO CONFIRM
- Unified bottom bar (carousel + embark aligned)
- HERO 1/4 at top
- Cards 80x100px
- Stat number lerp animation (PrimeTween)
- Embark voice line support
- Champion removed from 3D stage
- Ornamental dividers
- Decorative button top-highlights

WHAT FAILED:
- USS cannot render gradients, box-shadows, glows, or blur
- Flat colors look nothing like the mockup
- Logo backing is a flat black rectangle instead of radial gradient
- Buttons have no gradient fill, no glow, no depth
- Panel has no visible depth/shadow effect
- Everything looks "Roblox tier" per user feedback
</completed_work>

<remaining_work>
V2 PASS — C# VISUAL EFFECTS (must match mockup):

V2-1: UIGradientHelper — Generate Texture2D gradients at runtime
  - Button base gradient (dark brown top → darker bottom)
  - Button hover gradient (orange top → dark orange bottom)
  - Panel gradient (slightly lighter top → dark bottom)
  - Apply as background-image on VisualElements

V2-2: UIGlowOverlay — Layered glow VisualElements
  - Panel: outer shadow element + inner highlight element + hero-color top glow
  - Buttons: glow element behind on hover (orange, animated opacity)
  - Embark: breathing glow overlay

V2-3: UIDepthSystem — Premium panel depth
  - Multiple stacked semi-transparent VisualElements for shadow illusion
  - Inner highlight line at panel top
  - Gradient border simulation

V2-4: Demon interactive hover + idle animation (C#)
V2-5: Seamless video loop (video editing)
V2-6: Audio assets generation
V2-7: Champion model RenderTexture in panel
V2-8: Nav arrow icons (replace unicode)
</remaining_work>

<decisions_made>
- USS-only approach is PROVEN INSUFFICIENT for AAA visuals in UI Toolkit
- Must use C# runtime Texture2D generation + layered VisualElements
- Approved mockup is at: .superpowers/brainstorm/stable/aaa-v5.html
- Buttons: vertical right stack, all same size, dark base, orange on hover ONLY
- Stats: HP(red)/STAMINA-or-MANA(green/purple)/ATK(orange)/DEF(blue) as bars
- Attributes: STR/DEX/CON/INT/WIS/CHA/SPD as bold numbers (no bars)
- Champion: 50/50 split in panel, hero-only on 3D stage
- Font strategy: Cinzel Decorative=hero names, Cinzel=titles/values, Segoe UI=body
</decisions_made>

<blockers>
- UI Toolkit lacks CSS gradients, box-shadow, filter:blur — must use C# workarounds
- No audio assets exist yet (demon laughs, crying, music)
- Video loop has visible jump cut — needs re-editing
</blockers>

<context>
User spent weeks on UI with no concrete improvements. The HTML mockup got them genuinely excited ("PERFECTION LETS GET IT INSTALLED"). Then the Unity result was devastating ("TEEMU SHIT"). Trust is damaged.

The next session MUST deliver visible quality improvement. Start with the C# gradient texture system — this single change will transform flat buttons and panels into something that actually looks premium. Generate gradients, apply them, screenshot, verify BEFORE moving to the next task.

CRITICAL: Screenshot and visually verify after EVERY change. Do not batch-commit blind USS/code changes and assume they look right. The mockup is the north star — compare every screenshot against it.

Branch: feature/aaa-ui-overhaul (17 commits ahead of master)
Bug scan: 0 CRITICAL, 4 HIGH (all pre-existing, none from our changes)
Compile: 0 errors, 0 warnings
</context>

<next_action>
Start with: Create Assets/Scripts/UI/Core/UIGradientHelper.cs that generates Texture2D gradients. Apply to main menu buttons first. Screenshot. Compare to mockup. Only proceed if it actually looks better.
</next_action>
