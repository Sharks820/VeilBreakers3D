{
  "mcpServers": {
    "sequential-thinking": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Complex problem breakdown, multi-step reasoning, game balance calculations"
    },
    "memory-graph": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "mcp-knowledge-graph", "--memory-path", "C:/Users/Conner/.claude/memory"],
      "description": "Episodic memory / knowledge graph"
    },
    "serena": {
      "type": "stdio",
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/oraios/serena",
        "serena",
        "start-mcp-server"
      ],
      "description": "Symbol-aware code navigation and scoped edits"
    },
    "mcp-unity": {
      "type": "stdio",
      "command": "node",
      "args": ["Tools/mcp/launch-unity-mcp.js"],
      "description": "Unity Editor control - screenshots, scenes, GameObjects, builds, console output, prefabs, components"
    },
    "gemini-cli": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "mcp-gemini-cli", "--allow-npx", "--gemini-path", "C:/nvm4w/nodejs/gemini.cmd"],
      "env": {
        "PATH": "C:\\nvm4w\\nodejs;${PATH}"
      },
      "description": "Gemini CLI - search, chat, analyze files. Use for second opinions and web research."
    },
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub operations - PRs, issues, commits, CI status"
    },
    "blender": {
      "type": "stdio",
      "command": "uvx",
      "args": ["blender-mcp"],
      "env": {
        "BLENDER_PORT": "9876"
      },
      "description": "3D modeling - create/edit models, scenes, materials, render from Blender"
    },
    "image-process": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "image-process-mcp-server"],
      "description": "Crop, resize, rotate, convert image formats for game assets"
    },
    "notion": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@notionhq/notion-mcp-server"],
      "env": {
        "NOTION_API_KEY": "${NOTION_API_KEY}"
      },
      "description": "Project management - monster database, feature backlog, design docs"
    }
  },
  "_profile": {
    "name": "full",
    "purpose": "Include optional MCPs when actively doing Blender/image/notion tasks."
  }
}

